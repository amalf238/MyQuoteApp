<p style="color: green"><%= notice %></p>

<h1>Philosophers</h1>

<div id="philosophers">
  <% @philosophers.each do |philosopher| %>
    <%= render philosopher %>
    <p>
      <%= link_to "Show this philosopher", philosopher, class: 'btn btn-primary mt-1' %>
    </p>
  <% end %>
</div>

<%= link_to "New philosopher", new_philosopher_path, class: 'btn btn-secondary'%>

<p> </p>